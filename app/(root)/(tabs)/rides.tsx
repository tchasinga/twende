import React from 'react';
import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function componentName() {
  return (
    <SafeAreaView>
      <Text>This is the rides</Text>
     </SafeAreaView>
  );
}
